import { CourseEnquiryCount } from './course-enquiry-count';

describe('CourseEnquiryCount', () => {
  it('should create an instance', () => {
    expect(new CourseEnquiryCount()).toBeTruthy();
  });
});
