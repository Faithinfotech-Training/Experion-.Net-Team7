import { CourseEnquiryModel } from './course-enquiry-model';

describe('CourseEnquiryModel', () => {
  it('should create an instance', () => {
    expect(new CourseEnquiryModel()).toBeTruthy();
  });
});
