import { ResourceEnquiryModel } from './resource-enquiry-model';

describe('ResourceEnquiryModel', () => {
  it('should create an instance', () => {
    expect(new ResourceEnquiryModel()).toBeTruthy();
  });
});
